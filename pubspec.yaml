name: scd_tracker
description: Daily SCD Tracker

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  fl_chart: ^0.68.0
  intl: ^0.19.0

flutter:
  uses-material-design: true